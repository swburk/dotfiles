[general]
accounts = samburk
ui = ttyui
fsync = false

[Account samburk]
localrepository = samburk_local
remoterepository = samburk_remote
status_backend = sqlite
postsynchook = mu index -q -m ~/.mail

[Repository samburk_local]
type = Maildir
localfolders = ~/.mail/samburk
nametrans = lambda folder: {'archive': 'INBOX.Archive',
                            'drafts': 'INBOX.Drafts',
                            'sent': 'INBOX.Sent Items',
                            'junk': 'INBOX.Junk Mail',
                            'trash': 'INBOX.Trash',
                            }.get(folder, folder)

[Repository samburk_remote]
type = IMAP
remotehost = mail.messagingengine.com
remoteport = 993
remoteuser = smbk@fastmail.fm
ssl = yes
sslcacertfile = ~/.mutt/EntrustCA.crt
maxconnections = 2
nametrans = lambda folder: {'INBOX.Archive': 'archive',
                            'INBOX.Drafts': 'drafts',
                            'INBOX.Sent Items': 'sent',
                            'INBOX.Junk Mail': 'junk',
                            'INBOX.Trash': 'trash',
                            }.get(folder, folder)
